# Development log - 2026-09

## 2026-09-25 - Real terminal approval acceptance for pinned TUI

### Result

- The pinned TUI now has a repeatable Linux real-TTY acceptance for Allow once
  and Reject decisions.

### Why / context

- Strict dependency installation and a launch smoke check did not prove that
  approval decisions, tool results, and final answers survive in Session V4.

### Evidence

- pnpm test:tui:terminal passed with the exact DSH 0.1.7-rc.1 executable and
  frozen TUI 0.11.0 profile; both decisions, tool outcomes, final answers, and
  completed turns were checked.
- node --test tests/terminal-process.test.ts passed for already exited wrapper
  cleanup with a live descendant and bounded force-kill of an unresponsive
  process group.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/4>

### Follow-ups

- Long-session, cross-interface handoff, and cross-platform acceptance remain
  open in issue #4.

## 2026-09-25 - Report the generated Web contract identity

### Result

- Generate a Web Client and server identity from the single compatibility
  contract, including its parsed SHA-256 digest and all three pinned component
  identities.
- Reject stale generated identity before the Web package builds.

### Why / context

- A packaged Web extension must report which candidate graph it represents
  without creating another editable set of pins.

### Evidence

- Metadata drift test passed; typecheck, 21 tests, and native Chromium
  acceptance with DSH 0.1.7-rc.1 passed.
- The public source scan and contract check passed.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/5>

### Follow-ups

- Verify installed DSH, runtime-kit, and TUI graph bytes in image and installer
  assembly before activation.

## 2026-09-25 - Validate Web tool approvals and failed-turn recovery

### Result

- Expanded native browser acceptance to cover a pending turn, one allowed and
  one rejected Bash escalation, and a provider error across three isolated
  sessions.

### Why / context

- The additive Web plugin must preserve official DSH tool, approval, and error
  surfaces through Host restart.

### Evidence

- Pinned DSH 0.1.7-rc.1 and sandboxed Chromium passed the three-session browser
  run: allowed tool output, rejected-tool error, and provider invalid-request
  error remained visible after reopening their exact session IDs; six
  interaction and two error mock requests, zero page and Host errors.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/5>

### Follow-ups

- Issue #5 retains exact version reporting, image composition, and
  cross-platform evidence; issue #7 retains Web/TUI handoff.

## 2026-09-25 - Portable native Web browser acceptance

### Result

- Added a TypeScript browser acceptance command for the pinned DSH Web plugin
  using a disposable profile and authenticated local mock LLM.

### Why / context

- A real browser is required to validate session creation, copied IDs, history
  isolation, and recovery after Web Host restart.

### Evidence

- pnpm test:web:browser passed with pinned DSH 0.1.7-rc.1 and Chromium: two
  sessions, four mock requests, zero page and Host errors, and both histories
  recovered after restart.
- The disposable profile install used an isolated environment; Chromium ran
  with its sandbox enabled, and the mock rejected a request without its key.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/5>

### Follow-ups

- Issue #5 retains tool and approval continuity, TUI handoff, version
  reporting, and image composition gates.

## 2026-09-25 - Validate the native Web Client artifact

### Result

- The Web candidate now emits a DSH module-loader Client bundle with pinned
  React externals and a bounded package archive.

### Why / context

- The initial TypeScript output was plain ESM and could not be consumed by the
  DSH Client loader.

### Evidence

- Artifact registration, clipboard success and denial, metadata drift repair,
  strict peer installation, TypeScript checks, and publication scan passed.
- A disposable DSH 0.1.7-rc.1 Web profile served the packaged plugin with HTTP
  200, the Client script loaded, and no browser page errors.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/5>

### Follow-ups

- Issue 5 still requires two-session browser interaction, approval and tool
  continuity, image composition, and exact triple-version reporting.

## 2026-09-25 - Begin the native Web extension on DSH 0.1.7

### Result

- Added a TypeScript Web plugin candidate that contributes a session handoff
  action to an official header slot without changing DSH conversation
  ownership.

### Why / context

- The pinned DSH release exposes independently retained Session references, so
  the earlier addressed-session source patch is excluded from the new public
  product.

### Evidence

- Exact DSH peer graph resolved with strict peers; Web package compiled;
  metadata and registration checks passed. Browser acceptance remains open
  under issue 5.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/5>
- <https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.7-rc.1>

## 2026-09-25 - TypeScript is the primary source and test language

### Result

- Source and tests use TypeScript under a pinned compiler and Node type
  toolchain; stable JavaScript CLI entry points remain.
- The candidate moves to v0.1.0-rc.2 with Node >=24.3.0 as its runtime floor.

### Why / context

- The Workbench needs one checked development language for portable product
  code and its acceptance tests.
- Node 24.0 emitted an experimental type-stripping warning on the stable CLI;
  Node 24.3.0 runs the same CLI with clean stderr.

### Evidence

- Strict typecheck and all 16 tests passed with the frozen root lockfile;
  Node 24.3.0 CLI checks, public source scan, and CI are required before delivery.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/16>

## 2026-09-25 - Select service-stop session handoff topology

### Result

- Documented a shared DSH persistence root and canonical workspace with
  sequential writers; Web-to-TUI requires Web Host exit in the first milestone.

### Why / context

- Native Web retained a session writer after its viewing tab closed while
  another Web tab kept the Host active.

### Evidence

- In a disposable Linux fixture, TUI-to-Web and Web-to-TUI exact-ID resume
  preserved settled text and Bash tool turns; rejected concurrent resume left
  the compressed session log SHA-256 unchanged.
- A reviewed TUI profile lockfile and a fresh-home strict frozen install now
  keep the documented reproduction from resolving a new transitive graph.
- The locked profile also completed a second settled-text TUI-to-Web-to-TUI
  exact-ID handoff in the disposable workspace.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/6>

### Follow-ups

- Complete locked-profile tool continuity, executing-turn, approval,
  attachment, crash, platform, and migration-copy checks under issue #7
  before release acceptance.

## 2026-09-25 - Pin merged runtime-kit DSH 0.1.7 support

### Result

- The candidate Workbench graph now pins runtime-kit merge commit
  bd85605f30371522b529dac204dd7ea5db9198c3 and its exact Git tree.

### Why / context

- The previous candidate revision did not admit the selected official DSH 0.1.7
  release.

### Evidence

- Runtime-kit PR #272 passed its nine-check GitHub matrix, including pinned DSH
  0.1.7, upstream-next, retained 0.1.6 rollback, package validation, and macOS
  runtime health.

### Links

- <https://github.com/sympoies/dsh-runtime-kit/pull/272>
- <https://github.com/sympoies/dsh-workbench/issues/3>

### Follow-ups

- Complete Workbench Web/TUI composition and session handoff before accepting
  this release graph.

## 2026-09-25 - Scope the TUI peer correction to the candidate graph

### Result

- Workbench now renders exact pnpm peer overrides for dsh-working-activity
  0.4.0 from the DSH and TUI compatibility contract.

### Why / context

- The published activity plugin declares old DSH client and React 18 peers,
  preventing strict installation with DSH 0.1.7-rc.1 and TUI 0.11.0.

### Evidence

- Unmodified npm and pnpm strict resolution failed; an exact-parent override
  passed strict frozen installation with all three components and a disposable
  TUI profile reached the terminal UI.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/4>

### Follow-ups

- Complete real TTY resume, streaming, approval, and Web handoff acceptance
  before marking the tuple accepted.

## 2026-09-25 - Pin the first candidate component graph

### Result

- Added one machine-readable candidate contract for DSH, dsh-TUI, and
  dsh-runtime-kit, with Workbench release identity and a fail-closed activation
  gate.

### Why / context

- The package graph must retain immutable source and integrity identities while
  compatibility with DSH 0.1.7-rc.1 and cross-interface handoff remains
  unproven.

### Evidence

- Verified upstream Git tag or commit and tree identities, npm package
  integrity values, and package toolchain metadata on 2026-09-25; five contract
  tests and the publication source check passed.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/2>

### Follow-ups

- Implement installation and Web/TUI validation before changing the contract
  status to accepted.

## 2026-09-25 - Close publication scan bypasses

### Result

- The public scan now rejects opaque binary assets, checks public names, and
  hides matching bytes and names in diagnostics.

### Why / context

- Pre-merge review found compressed assets and path metadata could bypass
  content-only scanning.

### Evidence

- Synthetic fixtures for content categories, names, archives, source inventory,
  and unreadable trees passed; closed-set testing and red-team review found the
  repairs resolved.

### Links

- <https://github.com/sympoies/dsh-workbench/pull/13>

## 2026-09-25 - Protect public main before publication

### Result

- Activated a no-bypass main ruleset requiring pull requests and the source
  check, while blocking deletion and force pushes.

### Why / context

- A post-push scan cannot prevent private content from entering public Git
  history.

### Evidence

- GitHub effective branch-rule read-back lists deletion, non-fast-forward,
  pull-request, and required-status-check rules; PR #13 reports source as
  required and successful.

### Links

- <https://github.com/sympoies/dsh-workbench/pull/13>

## 2026-09-25 - Define public product and publication boundary

### Result

- Added portable product and private deployment ownership, MIT license, and a
  public-source scan gate.

### Why / context

- The new public repository needs an explicit boundary before importing source
  or shipping artifacts.

### Evidence

- Verified the selected DSH, dsh-TUI, and runtime-kit source licenses are MIT;
  source scan passed and rejected a disposable email fixture without printing
  its contents.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/1>
- <https://github.com/sympoies/dsh-workbench/issues/8>

## 2026-09-24 - Initialize the public DSH Workbench repository

### Result

- Established portable product boundaries, contributor guidance, and the
  development log.

### Why / context

- A new repository history keeps the public product separate from private
  deployment settings and legacy state.

### Evidence

- The public roadmap and bootstrap issue are open; devlog index and devlog
  check passed on the scaffold.

### Links

- <https://github.com/sympoies/dsh-workbench/issues/11>
