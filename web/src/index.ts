/** The Web contribution has no server-side session or transport implementation. */
export function apply(): void {}

/** Exact contract identity available to the profile assembler and diagnostics. */
export { workbenchIdentity } from './identity.js';
